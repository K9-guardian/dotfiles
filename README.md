# Dotfiles

Ansible collection for my dotfiles.

Usage:
- Run `setup.sh` in `dotfiles-core` to create the ansible virtual environment.
- Create `~/.passfiles/ansible_dotfiles` with ansible vault password.
- Run `make <thinkpad|mimir|pavillion>`.
