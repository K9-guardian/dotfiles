- name: Execute core kitty tasks
  include_role:
    name: kitty_core

- name: Copy thinkpad kitty configuration
  ansible.posix.synchronize:
    src: "config/"
    dest: "~/.config/kitty"
    group: false
    times: false
    checksum: true
