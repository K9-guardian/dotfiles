- name: Execute core neovim tasks
  include_role:
    name: core_neovim

- name: Copy personal neovim configuration
  ansible.posix.synchronize:
    src: "config/"
    dest: "~/.config/nvim/"
    times: false
    checksum: true
